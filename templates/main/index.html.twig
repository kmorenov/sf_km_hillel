{% extends 'base.html.twig' %}

{% block title %} Articles {% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
    {#Parent Start#}
    {#{{ parent() }}#}
    {#Parent Finish#}
    {% block content %}
        Content
        <a href="/admin">SONATA ADMIN</a>
        <div class="example-wrapper">
            <h1> Articles! ✅<a href="{{ path('article_index') }}">CRUD - Articles Index</a></h1>

            {% dump(articles) %}
            <ul>
                {% for article in articles %}
                    <li><a href="{{ path('article', {'id' : article.id}) }}"> {{ article.title }} </a></li>
                {% endfor %}
            </ul>
            <a href="{{ path('categories') }}">Categories</a>
        </div>
    {% endblock %}
{% endblock %}
